#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@vscode/test-cli/out/bin.mjs" "$@"
  ret=$?
else 
  node  "$basedir/../@vscode/test-cli/out/bin.mjs" "$@"
  ret=$?
fi
exit $ret
